# PyPI Nonpublication Proof

The PyPI JSON endpoint for package `xslim`, version `2.1.2+riscy.1`, returned
HTTP 404 at 0, 15, 30, 60, and 120 seconds after the GitHub release became
public. GitHub Actions remained disabled and workflow inventory/run counts
remained zero. No PyPI upload, trusted-publishing, or OIDC publication command
was executed.

Status: `pass`.
